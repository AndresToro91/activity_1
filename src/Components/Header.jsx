

export const Header = () => {/* Componente para el encabezado de la página */
    return (/* Se retorna el encabezado */
        <header className="header">
            <div className="header-container">
                <img src={'/img/Relatos.png'} alt="inicio" className="header-img"/>
            </div>

        </header>
    );
}